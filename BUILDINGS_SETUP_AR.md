# 🏢 دليل إعداد قاعدة بيانات المباني
## شركة الفؤاد للاستثمار العقاري

---

## 🎯 ما تم إنجازه

### ✅ نظام إدارة مباني متكامل

تم إنشاء نظام شامل لإدارة المباني يتضمن:

1. **معلومات المبنى الكاملة**
   - صورة غلاف رئيسية
   - الاسم بالعربية والإنجليزية
   - الوصف بالعربية والإنجليزية
   - العنوان بالعربية والإنجليزية
   - الموقع على الخريطة (خطوط الطول والعرض)

2. **المواصفات التفصيلية**
   - عدد الطوابق
   - إجمالي الوحدات
   - الوحدات المتاحة
   - مساحة المبنى (متر مربع وقدم مربع)
   - عدد مواقف السيارات
   - حالة المبنى (متاح / مباع / قريباً)
   - حالة التسليم (تحت الإنشاء / جاهز للسكن / تم التسليم)

3. **معرض الصور**
   - صور متعددة غير محدودة
   - تصنيف الصور (خارجي / داخلي / مرافق / مخططات)
   - عناوين ووصف بالعربية والإنجليزية
   - ترتيب العرض
   - الصور المميزة

4. **الجدول الزمني للمشروع**
   - مراحل البناء المختلفة
   - نسبة الإنجاز لكل مرحلة (0-100%)
   - حالة كل مرحلة (لم تبدأ / جارية / مكتملة / متأخرة)
   - تواريخ البداية والنهاية
   - أيقونة لكل مرحلة
   - تمييز المرحلة الحالية

---

## 📁 الملفات المنشأة

### ملفات SQL
- `/supabase/migrations/buildings_complete.sql` - ملف الـ Migration الرئيسي
- `/supabase/migrations/BUILDINGS_DATABASE_GUIDE.md` - الدليل الشامل بالإنجليزية

### ملفات React Components
- `/components/BuildingDetailModal.tsx` - نافذة عرض تفاصيل المبنى
- `/components/BuildingsMap.tsx` - تم تحديثه لدعم البيانات الجديدة

### ملفات التوثيق
- `/BUILDINGS_SETUP_AR.md` - هذا الملف (الدليل السريع بالعربية)

---

## 🗄️ جداول قاعدة البيانات

### 1. جدول `buildings` (تم التحديث)
الجدول الرئيسي لمعلومات المباني

**الحقول الجديدة المضافة:**
- `name_ar` - اسم المبنى بالعربية
- `description_ar` - وصف المبنى بالعربية
- `address_ar` - العنوان بالعربية
- `cover_photo` - صورة الغلاف الرئيسية
- `building_area_sqm` - المساحة بالمتر المربع
- `building_area_sqft` - المساحة بالقدم المربع
- `parking_spaces` - عدد مواقف السيارات
- `delivery_status` - حالة التسليم
- `construction_start_date` - تاريخ بدء البناء
- `completion_date` - تاريخ الإنجاز المتوقع
- `status_ar` - الحالة بالعربية

### 2. جدول `building_gallery` (جديد)
معرض صور المباني

**الحقول الرئيسية:**
- `building_id` - معرف المبنى
- `image_url` - رابط الصورة
- `image_title_en` / `image_title_ar` - عنوان الصورة
- `image_description_en` / `image_description_ar` - وصف الصورة
- `display_order` - ترتيب العرض
- `image_type` - نوع الصورة (خارجي / داخلي / مرافق)
- `is_featured` - صورة مميزة

### 3. جدول `building_timeline_phases` (جديد)
مراحل المشروع والجدول الزمني

**الحقول الرئيسية:**
- `building_id` - معرف المبنى
- `phase_name_en` / `phase_name_ar` - اسم المرحلة
- `phase_description_en` / `phase_description_ar` - وصف المرحلة
- `phase_order` - ترتيب المرحلة
- `start_date` / `end_date` - تواريخ البداية والنهاية
- `completion_percentage` - نسبة الإنجاز (0-100)
- `phase_status` - حالة المرحلة
- `milestone_icon` - أيقونة المرحلة
- `is_current_phase` - المرحلة الحالية

---

## 🚀 خطوات التفعيل (5 دقائق)

### الخطوة 1: فتح Supabase

1. اذهب إلى https://supabase.com/dashboard
2. اختر مشروعك
3. اضغط على **SQL Editor** من القائمة الجانبية

### الخطوة 2: تشغيل الـ Migration

1. افتح الملف: `/supabase/migrations/buildings_complete_fixed.sql` ⚠️ استخدم هذا الملف
2. انسخ **كل المحتوى** (الملف بالكامل)
3. الصق في SQL Editor
4. اضغط **RUN** أو **تشغيل**
5. انتظر رسالة "Success ✓"

### الخطوة 3: التحقق من النجاح

شغل هذا الاستعلام للتأكد:

```sql
SELECT 
  (SELECT COUNT(*) FROM buildings) as عدد_المباني,
  (SELECT COUNT(*) FROM building_gallery) as عدد_الصور,
  (SELECT COUNT(*) FROM building_timeline_phases) as عدد_المراحل;
```

**النتيجة المتوقعة:**
- عدد المباني: 3
- عدد الصور: 9 (3 صور لكل مبنى)
- عدد المراحل: 15 (5 مراحل لكل مبنى)

---

## 💡 أمثلة الاستخدام

### إضافة مبنى جديد

```sql
INSERT INTO buildings (
  name,
  name_ar,
  description,
  description_ar,
  address,
  address_ar,
  latitude,
  longitude,
  floors,
  total_units,
  available_units,
  status,
  status_ar,
  cover_photo,
  building_area_sqm,
  parking_spaces,
  delivery_status,
  project_id
) VALUES (
  'Luxury Heights',
  'أبراج لوكشري هايتس',
  'Premium residential towers with stunning views',
  'أبراج سكنية فاخرة بإطلالات خلابة',
  'New Cairo, Egypt',
  'القاهرة الجديدة، مصر',
  30.0444,
  31.2357,
  25,
  120,
  50,
  'available',
  'متاح',
  'https://images.unsplash.com/photo-1545324418-cc1a3fa10c00?w=800',
  12000.00,
  180,
  'under_construction',
  (SELECT id FROM projects LIMIT 1)
);
```

### إضافة صور للمعرض

```sql
-- احصل على ID المبنى أولاً
-- استبدل 'معرف-المبنى' بالـ UUID الفعلي

INSERT INTO building_gallery (
  building_id,
  image_url,
  image_title_en,
  image_title_ar,
  display_order,
  image_type,
  is_featured
) VALUES
-- الصورة 1: المنظر الخارجي
(
  'معرف-المبنى',
  'https://images.unsplash.com/photo-1545324418-cc1a3fa10c00?w=800',
  'Building Exterior',
  'المنظر الخارجي للمبنى',
  1,
  'exterior',
  true
),
-- الصورة 2: الردهة
(
  'معرف-المبنى',
  'https://images.unsplash.com/photo-1600607687939-ce8a6c25118c?w=800',
  'Modern Lobby',
  'الردهة العصرية',
  2,
  'interior',
  false
),
-- الصورة 3: حمام السباحة
(
  'معرف-المبنى',
  'https://images.unsplash.com/photo-1576013551627-0cc20b96c2a7?w=800',
  'Swimming Pool',
  'حمام السباحة',
  3,
  'amenity',
  false
);
```

### إضافة مراحل المشروع

```sql
INSERT INTO building_timeline_phases (
  building_id,
  phase_name_en,
  phase_name_ar,
  phase_description_en,
  phase_description_ar,
  phase_order,
  start_date,
  end_date,
  completion_percentage,
  phase_status,
  milestone_icon,
  is_current_phase
) VALUES
-- المرحلة 1: الأساسات
(
  'معرف-المبنى',
  'Foundation Work',
  'أعمال الأساسات',
  'Ground preparation and foundation',
  'تجهيز الأرض والأساسات',
  1,
  '2024-01-01',
  '2024-06-30',
  100,
  'completed',
  'Layers',
  false
),
-- المرحلة 2: البناء
(
  'معرف-المبنى',
  'Construction',
  'أعمال البناء',
  'Main structure construction',
  'بناء الهيكل الرئيسي',
  2,
  '2024-07-01',
  '2025-03-31',
  60,
  'in_progress',
  'Building2',
  true
),
-- المرحلة 3: التشطيبات
(
  'معرف-المبنى',
  'Finishing',
  'التشطيبات',
  'Interior finishing works',
  'أعمال التشطيبات الداخلية',
  3,
  '2025-04-01',
  '2025-12-31',
  0,
  'not_started',
  'Paintbrush',
  false
);
```

---

## 🔧 التحديثات الشائعة

### تحديث عدد الوحدات المتاحة

```sql
UPDATE buildings
SET available_units = 45
WHERE name = 'Luxury Heights';
```

### تحديث نسبة إنجاز مرحلة

```sql
UPDATE building_timeline_phases
SET 
  completion_percentage = 75,
  phase_status = 'in_progress'
WHERE phase_name_ar = 'أعمال البناء';
```

### تحديث حالة التسليم

```sql
UPDATE buildings
SET 
  delivery_status = 'ready_to_move',
  completion_date = '2025-12-31'
WHERE name = 'Luxury Heights';
```

### إضافة صورة جديدة

```sql
INSERT INTO building_gallery (
  building_id,
  image_url,
  image_title_ar,
  display_order,
  image_type
) VALUES (
  'معرف-المبنى',
  'https://new-image.jpg',
  'صورة جديدة',
  4,
  'general'
);
```

---

## 📊 استعلامات مفيدة

### عرض كل المباني مع التفاصيل

```sql
SELECT 
  name as الاسم,
  name_ar as الاسم_بالعربية,
  floors as الطوابق,
  total_units as إجمالي_الوحدات,
  available_units as الوحدات_المتاحة,
  delivery_status as حالة_التسليم
FROM buildings
ORDER BY name;
```

### عرض المباني مع عدد الصور

```sql
SELECT 
  b.name_ar as المبنى,
  COUNT(g.id) as عدد_الصور
FROM buildings b
LEFT JOIN building_gallery g ON b.id = g.building_id
GROUP BY b.id, b.name_ar
ORDER BY عدد_الصور DESC;
```

### عرض مراحل مبنى معين

```sql
SELECT 
  phase_name_ar as المرحلة,
  completion_percentage as نسبة_الإنجاز,
  phase_status as الحالة,
  is_current_phase as المرحلة_الحالية
FROM building_timeline_phases
WHERE building_id = 'معرف-المبنى'
ORDER BY phase_order;
```

### حساب نسبة إنجاز المبنى الإجمالية

```sql
SELECT 
  b.name_ar as المبنى,
  ROUND(AVG(t.completion_percentage)) as نسبة_الإنجاز_الإجمالية
FROM buildings b
JOIN building_timeline_phases t ON b.id = t.building_id
GROUP BY b.id, b.name_ar;
```

---

## 🎨 الأيقونات المتاحة للمراحل

عند تحديد `milestone_icon`، استخدم هذه الأسماء:

**متعلقة بالبناء:**
- `Pickaxe` - الحفر
- `Layers` - الأساسات
- `Building2` - الهيكل
- `Home` - السكن
- `Wrench` - التركيبات
- `Hammer` - البناء

**متعلقة بالتقدم:**
- `CheckCircle` - الإنجاز
- `Clock` - جاري التنفيذ
- `Calendar` - الجدولة
- `Target` - الأهداف

**المميزات:**
- `Zap` - الأنظمة الكهربائية
- `Paintbrush` - التشطيبات
- `Key` - التسليم
- `Settings` - التشغيل
- `PenTool` - التصميم

القائمة الكاملة: https://lucide.dev/icons/

---

## 🎯 حالات المراحل

### `phase_status` - حالة المرحلة

- `not_started` - لم تبدأ
- `in_progress` - جارية
- `completed` - مكتملة
- `delayed` - متأخرة
- `on_hold` - متوقفة مؤقتاً

### `delivery_status` - حالة التسليم

- `under_construction` - تحت الإنشاء
- `ready_to_move` - جاهز للسكن
- `delivered` - تم التسليم

### `status` - حالة المبنى

- `available` - متاح
- `sold_out` - مباع بالكامل
- `coming_soon` - قريباً

---

## ✅ قائمة التحقق

بعد التنفيذ، تأكد من:

- [ ] تم تشغيل الـ SQL Migration بنجاح
- [ ] يوجد 3 مباني نموذجية في قاعدة البيانات
- [ ] كل مبنى لديه 3 صور في المعرض
- [ ] كل مبنى لديه 5 مراحل في الجدول الزمني
- [ ] الخريطة تعرض المباني بشكل صحيح
- [ ] النقر على أيقونة المبنى يعرض التفاصيل
- [ ] المحتوى بالعربية والإنجليزية يعمل

---

## 🔍 التحقق من البيانات

### تحقق من المباني

```sql
SELECT COUNT(*) as عدد_المباني FROM buildings;
```

**المتوقع:** 3 أو أكثر

### تحقق من الصور

```sql
SELECT COUNT(*) as عدد_الصور FROM building_gallery;
```

**المتوقع:** 9 أو أكثر

### تحقق من المراحل

```sql
SELECT COUNT(*) as عدد_المراحل FROM building_timeline_phases;
```

**المتوقع:** 15 أو أكثر

---

## 🆘 حل المشاكل

### المشكلة: الـ Migration فشل

**الحل:**
1. تأكد من نسخ الملف بالكامل
2. تحقق من صلاحيات Admin في Supabase
3. حاول حذف الجداول وإعادة التشغيل

### المشكلة: المباني لا تظهر على الخريطة

**الحل:**
```sql
-- تحقق من الإحداثيات
SELECT name, latitude, longitude FROM buildings;

-- أضف إحداثيات إذا كانت مفقودة
UPDATE buildings
SET latitude = 30.0444, longitude = 31.2357
WHERE latitude IS NULL;
```

### المشكلة: الصور لا تحمل

**الحل:**
```sql
-- تحقق من روابط الصور
SELECT image_url FROM building_gallery LIMIT 5;

-- تأكد من صحة الروابط
```

---

## 📞 الدعم الفني

إذا واجهت أي مشاكل:

1. راجع الدليل الشامل: `/supabase/migrations/BUILDINGS_DATABASE_GUIDE.md`
2. تحقق من الـ console في المتصفح للأخطاء
3. راجع لوقات Supabase

---

## 🎊 مؤشرات النجاح

ستعرف أن كل شيء يعمل عندما:

1. ✅ الخريطة تعرض علامات المباني
2. ✅ النقر على علامة يفتح تفاصيل المبنى
3. ✅ تظهر صور المعرض (3 صور لكل مبنى)
4. ✅ يظهر الجدول الزمني مع نسب الإنجاز
5. ✅ المحتوى بالعربية والإنجليزية يعمل
6. ✅ جميع الإحصائيات تظهر (الطوابق، الوحدات، المساحة)
7. ✅ زر "الحصول على الاتجاهات" يفتح خرائط جوجل

---

## 📚 الملفات المرجعية

- **ملف SQL:** `/supabase/migrations/buildings_complete.sql`
- **الدليل الشامل:** `/supabase/migrations/BUILDINGS_DATABASE_GUIDE.md`
- **نافذة التفاصيل:** `/components/BuildingDetailModal.tsx`
- **مكون الخريطة:** `/components/BuildingsMap.tsx`

---

**حالة قاعدة البيانات:** ✅ جاهزة  
**المكونات:** ✅ مكتملة  
**التوثيق:** ✅ شامل  
**التاريخ:** 24 يناير 2026

---

## 🚀 الخطوات التالية

1. ✅ تشغيل الـ SQL Migration
2. ⏳ التحقق من البيانات
3. ⏳ اختبار الخريطة والتفاصيل
4. ⏳ إضافة مباني جديدة حسب الحاجة
5. ⏳ تخصيص المحتوى

**بالتوفيق! 🎉**